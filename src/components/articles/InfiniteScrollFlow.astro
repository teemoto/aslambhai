---
const steps = ["Existing DOM", "user nears bottom", "fetch more content", "create new slot", "request ad", "render"];
---

<ol class="infinite-scroll-flow">
  {steps.map((step) => <li>{step}</li>)}
</ol>
